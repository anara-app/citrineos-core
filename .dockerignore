# Ignore node modules directories
**/node_modules

# Ignore distribution directories (lib for backwards compatibility)
**/dist
**/lib

# Common ignore files
.gitignore
# NOTE: keep the root lockfile to make Docker builds deterministic.
# (Still ignore any nested lockfiles if they exist.)
**/package-lock.json
!package-lock.json

# Specific files and file types to ignore
**/*.js
**/*.cjs
**/*.md
**/*.yml
**/*.tsbuildinfo
**/*.log
**/*.tgz
**/*.Dockerfile

# Specific directories to ignore
**/DirectusExtensions
**/Swarm
**/data

# include sequelize bridge
!Server/
!Server/src/
!Server/src/config/
!Server/src/config/sequelize.bridge.config.js
